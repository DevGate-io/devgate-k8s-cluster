apiVersion: apps/v1
kind: Deployment
metadata:
  name: devgate-catalog-service
  namespace: devgate
  labels:
    app: devgate-catalog-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devgate-catalog-service
  template:
    metadata:
      labels:
        app: devgate-catalog-service
    spec:
      containers:
        - name: catalog-service
          image: devgate/catalog-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          env:
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DB
              value: devgate_catalog
            - name: POSTGRES_USERNAME
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: devgate-secrets
                  key: POSTGRES_PASSWORD
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: RABBITMQ_PORT
              value: "5672"
            - name: RABBITMQ_USERNAME
              value: guest
            - name: RABBITMQ_PASSWORD
              value: guest
            - name: JWT_SECRET_BASE64
              valueFrom:
                secretKeyRef:
                  name: devgate-secrets
                  key: JWT_SECRET_BASE64
          readinessProbe:
            tcpSocket:
              port: 8084
            initialDelaySeconds: 40
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 20
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: devgate-catalog-service
  namespace: devgate
  labels:
    app: devgate-catalog-service
spec:
  selector:
    app: devgate-catalog-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
